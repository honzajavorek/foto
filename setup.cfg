[wheel]
universal = 1

[flake8]
exclude = env

[tool:pytest]
testpaths = tests
norecursedirs = env venv .git
addopts = --cov=foto

[aliases]
test = pytest

[semantic_release]
version_variable = setup.py:version
